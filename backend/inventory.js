/*
 * Inventory Class File
*/

class inventory {
    constructor() {
        this.money = 100, 000, 000;
    }

    addShare(searchTerm, amount) {
        if (stocks[searchTerm] === undefined) {
            stocks[searchTerm] = amount; 
        } else {
            stocks[searchTerm] = amount; 
        }
           // term does exist
    }

    removeShare
}

stocks = new Object();
stocks["h"] = 1;
console.log(stocks["hi"]);